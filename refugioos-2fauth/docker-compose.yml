version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: refugioos-2fauth_app_1
      APP_PORT: "8000"
      PROXY_AUTH_ADD: "false"

  app:
    image: ghcr.io/refugioos/2fauth:8.0.2-refugioos.1
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/2fauth